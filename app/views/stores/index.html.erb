<h1>Listing stores</h1>

<table>
  <thead>
    <tr>
      <th>Product name</th>
      <th>Brand</th>
      <th>Description</th>
      <th>Color</th>
      <th>Price</th>
      <th>Feature1</th>
      <th>Feature2</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @stores.each do |store| %>
      <tr>
        <td><%= store.product_name %></td>
        <td><%= store.brand %></td>
        <td><%= store.description %></td>
        <td><%= store.color %></td>
        <td><%= store.price %></td>
        <td><%= store.feature1 %></td>
        <td><%= store.feature2 %></td>
        <td><%= link_to 'Show', store %></td>
        <td><%= link_to 'Edit', edit_store_path(store) %></td>
        <td><%= link_to 'Destroy', store, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
 <%= will_paginate @stores %>
<%= link_to 'New Store', new_store_path %>
